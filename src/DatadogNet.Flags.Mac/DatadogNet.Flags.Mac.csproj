<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <DatadogFramework>DatadogFlags</DatadogFramework>
    <DatadogPackageName>Flags</DatadogPackageName>
    <Description>.NET for Mac Catalyst / .NET MAUI bindings for the native Datadog iOS SDK's DatadogFlags framework: feature-flag evaluation and exposure reporting. Built against dd-sdk-ios $(DatadogNativeVersion). NOTE: upstream exposes no Objective-C API for this module yet, so it ships the native framework but no callable C# surface - see the package README.</Description>
    <PackageTags>datadog-flags;feature-flags</PackageTags>
  </PropertyGroup>

  <Import Project="../Datadog.Binding.props" />

  <ItemGroup>
    <ProjectReference Include="../DatadogNet.Core.Mac/DatadogNet.Core.Mac.csproj" />
    <ProjectReference Include="../DatadogNet.Internal.Mac/DatadogNet.Internal.Mac.csproj" />
  </ItemGroup>

</Project>
